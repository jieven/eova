function test() {

    // 默认新增到根下
    // let menuId = uzoo.app.treeId.value;
    //
    // me.layer.confirm('未选择父目录,确认添加到根目录下?', () => {
    //     let url = '/menu/toAdd?view=enjoy&parent_id=' + menuId;
    //     me.layer.open('新增菜单', url, () => {
    //         me.layer.msg('操作成功！')
    //     }, ()=>{}, '1000px', '600px')
    // })

    me.layer.msg('测试按钮.....');

}